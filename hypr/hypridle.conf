general {
    lock_cmd = pidof hyprlock || uwsm app -- hyprlock
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
    ignore_dbus_inhibit = false
}

# Примечание: sleep/suspend на этой машине замаскированы, поэтому
# реального ухода в сон нет — здесь только затемнение и блокировка по idle.

listener {
    timeout = 300
    on-timeout = brightnessctl -s set 10%
    on-resume = brightnessctl -r
}

# АВТО-ЛОК ПО ТАЙМЕРУ ОТКЛЮЧЁН — блокировка только вручную SUPER+L.
# listener {
#     timeout = 600
#     on-timeout = loginctl lock-session
# }

# АВТО-DPMS (гашение экрана = «хранитель экрана») ОТКЛЮЧЁН —
# экран сам не гаснет и не блокируется по таймеру; блокировка только вручную SUPER+L.
# listener {
#     timeout = 660
#     on-timeout = hyprctl dispatch dpms off
#     on-resume = hyprctl dispatch dpms on
# }
